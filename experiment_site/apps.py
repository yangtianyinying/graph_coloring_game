from django.apps import AppConfig


class ExperimentSiteConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "experiment_site"
    verbose_name = "Graph coloring experiment site"
