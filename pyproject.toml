[project]
name = "graph-coloring-experiment"
version = "0.1.0"
description = "图着色推断实验 — 纯静态前端（GitHub Pages）"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
